#include "stdafx.h"
#include <iostream>	
#include "conio.h"
#define  MAX 100
using namespace std;
void cargarvector (double a[],int tam);
void mostrarvector (double a[],int tam);
double promedio (double a[], int tam);
void main()
{
	double S[MAX];
	int N;
	double prom;

	do{
		cout<<"ingrese el numero de elementos del vector: ";
		cin>>N; 
	}
		while (N> MAX|| N <=0); 
		cargarvector(S,N);
		for (int i=0;i<N;i++);
		cout<<"El promedio: "<<prom<<endl;
		getch();
}
		void cargarvector (double a [ ],int tam)
		{
			for (int i=0;i<N;i++)
				cout<<"vector["<<i<<"]=";
				cin>>a[i];
				} 
		void mostarvector (double v [i],int tam)
		{
			int s, prom;
			s=0;
				for(int i=0;i<tam;i++)	
					s=s+v[i];
		}
		prom=s/n;
		return prom;
		}
